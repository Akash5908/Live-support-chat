export enum Status {
  OPEN = "open",
  ASSIGNED = "assigned",
  CLOSED = "closed",
}

export enum Role {
  Admin = "admin",
  Supervisor = "supervisor",
  Agent = "agent",
  Candidate = "candidate",
}
